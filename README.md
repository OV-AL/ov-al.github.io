# ov-al.github.io

The site is intentionally offline (2026-07-09). The content is built from the private OV-AL/oval repo's
`site/` and published here by its `site-deploy` workflow — currently disabled. Re-enable the workflow and
dispatch it to bring the site back.
